using Microsoft.Extensions.DependencyInjection;

using {domainRootNs}.Features.{nameofPlural}.Service;
using {domainRootNs}.Configurations.Injector;

namespace {domainRootNs}.Features.{nameofPlural};

internal sealed class {itemName}DependencyResolver : IInjectServices
{
    public void Configure(IServiceCollection services)
    {
        services.AddScoped<{itemName}Service>();
    }
}